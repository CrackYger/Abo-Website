import React from 'react'
export function Th({ children }){ return <th className="px-3 py-2 text-left text-xs font-semibold uppercase tracking-wide text-zinc-500">{children}</th> }
export function Td({ children }){ return <td className="px-3 py-3 align-top">{children}</td> }
